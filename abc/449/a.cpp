#include <bits/stdc++.h>
using namespace std;

#define pi numbers::pi

int main(void) {
    double d;
    cin >> d;
    double r = d / 2;
    double a = (r *r) * pi;
    cout << fixed << setprecision(6);
    cout << a << endl;
}